# CLAUDE.md

給 Claude Code 的專案指引。本檔重點為 **Context 管理規範**,整理自
《Claude Code Context 管理》一文的實務做法。

## 專案簡介

本 repo 為 GitHub 個人檔案倉庫(`verna0519/verna0519`),`README.md`
會顯示在 GitHub 個人頁面。變更以維護 profile 內容為主。

## Context 管理規範

### 何時壓縮 / 重設

- **切換到「不相關的新任務」時,先 `/clear`**,不要延用舊 context。
  舊任務的檔案內容、探索結果留在 context 只會佔用 token 且干擾判斷。
- **同一任務但對話過長、token 逼近上限時,用 `/compact`** 壓縮歷史,
  保留任務目標與關鍵決策即可。
- 需要回到先前某個狀態時,用 `/rewind` 而不是靠記憶重述。

### 壓縮 (Compaction) 行為指示

當進行自動或手動壓縮時,務必保留以下資訊,其餘可省略:

- 目前任務的**目標與驗收條件**
- 已做出的**關鍵決策與理由**
- **尚未完成**的待辦項目
- 使用者明確表達的**偏好與限制**

可安全捨棄:大段檔案原文、已被取代的過時方案、逐輪的探索過程細節。

### 節省 Context 的做法

- **廣泛搜尋、跨多檔案的探索工作,交給 subagent**,只帶回結論,
  不要把整批檔案原文倒進主對話。
- 讀檔時**只讀需要的片段**,已知範圍就用 offset/limit,不要整檔讀入。
- **控制 MCP server 數量**:只啟用當前任務需要的 server,
  過多的工具定義會持續佔用 context。
- 不要為了驗證而重讀剛編輯過的檔案 —— 編輯工具失敗會報錯。

### 監控 token 用量

- 對長工作階段,適時用 `/context` 查看 context 佔用、`/cost` 查看花費。
- 感覺回應變慢或開始遺漏先前資訊時,通常代表 context 過長,
  應考慮 `/compact` 或 `/clear`。

## 常見錯誤(避免)

- 一路 `/compact` 卻從不 `/clear`:不相關任務堆疊會累積雜訊。
- 把大型檔案整份貼入對話,而非讓工具按需讀取。
- 同時掛載一堆用不到的 MCP server。
